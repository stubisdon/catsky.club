# Env vars reference. Copy to .env for local overrides (gitignored).
#
# DEV:  .env.development + .env.development.local. Portal uses localhost; Vite proxies /ghost and /members to Ghost.
# PROD: deploy.sh on server uses .env.server and exports VITE_* before npm run build
# Server-only vars (GHOST_ADMIN_API_KEY, etc.): see ENV_SERVER.example
#
# Frontend (Vite / Ghost Portal)
VITE_GHOST_URL=https://catsky.club
VITE_GHOST_CONTENT_API_KEY=your_content_api_key_here
# Dev-only: where to proxy Ghost API (vite.config uses this)
# VITE_GHOST_API_PROXY=https://catsky.club
