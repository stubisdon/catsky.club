name: Run Tests

on:
  # Tests are temporarily disabled in CI.
  # (We keep this workflow file so it’s easy to re-enable later.)
  workflow_dispatch:

jobs:
  tests-disabled:
    name: Tests disabled
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: |
          echo "Tests are temporarily disabled in CI. (No-op workflow)"
          exit 0
